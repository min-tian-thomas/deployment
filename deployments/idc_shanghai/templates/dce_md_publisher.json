{
    "logging": {
        "log_level": "Info",
        "log_cpu": "{{log_cpu}}"
    },
    "event_loops": [
        {
            "name": "main_loop",
            "cpu_id": "{{main_loop_cpu}}",
            "busy_spin": true
        },
        {
            "name": "admin_loop",
            "cpu_id": "{{admin_loop_cpu}}",
            "busy_spin": false
        }
    ],
    "listen_nic": "{{listen_nic}}",
    "listen_port": "{{listen_port}}"
}
